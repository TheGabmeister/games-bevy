Place optional runtime assets here.

Current supported asset files:

- `music_spaceshooter.ogg`: looped during `AppState::Playing`

The game now runs without this file, but audio is skipped until it is added.
